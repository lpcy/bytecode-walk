        <span>{{if pad}}pad:{{hit pad/}}{{/if}}</span>
        <span>default:{{hit default/}}</span>
        <span>npairs:{{hit npairs/}}</span>
        {{for pairs}}
            {{hit A/}}:{{hit B/}}
        {{/for}}
